"use strict"; //top of the script, strict mode.
//should make variable with type.
//should make function with type.
//should make class with type.
//should make interface with type.
//should make enum with type.
//should make type with type.
//should make namespace with type.
//should make module with type.

function add(x,y){
    return x+y;
}
console.log(add(4,5));

